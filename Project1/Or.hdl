// Name: Freddy Velasco 
// UIN:  327004703

 /**
 * Or gate:
 * out = 1 if (a == 1 or b == 1)
 *       0 otherwise
 */

CHIP Or {
    IN a, b;
    OUT out;

    PARTS:
    // Simplified Boolean Expression: out: (a nor b)'
    Nor(a=a, b=b, out=temp);
    Not(in=temp, out=out);
}
